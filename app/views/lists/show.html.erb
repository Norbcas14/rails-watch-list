<%= render 'header' %>

<div class="container">
 <div class="movie-content">
  <div class="row">
    <div class="col-lg-6">

    </div>
    <div class="col-lg-6">
      <h2><%= @list.name %> Movies</h2>
      <div class="btn-style">
        <p><%= link to "Bookmark", new_list_bookmark_path(@list), class: "btn-hook" %></p>
        <p><%= link to "Edit", edit_list_path(@list), class: "btn-edit" %></p>
        <p><%= link to "Delete", edit_list_path(@list), class: "btn-delete" %></p>
      </div>
      <p>Lorem ipsum, dolor sit amet consectetur adipisicing elit. Rem quidem eligendi laudantium ut
       doloribus laboriosam minima iure, recusandae aut. Necessitatibus quibusdam sint sequi ad, laboriosam
       quae quos repudiandae qui blanditiis!</p>
    </div>

  </div>
    <hr>

    <div class="movie-gird">
      <% @list.movies.each do |movie| %>
      <div class="box-poster">
        <img src="<%= movie.poster_url %>">
        <p class="movie-path"><%= link_to movie.title, movie_path(movie)</p>
      </div>
      <% end %>
    </div>
 </div>
</div>
